import React from 'react'

export default () => {
    return(
        <div>
            <h3>Starting a react project:</h3>

            <p>Create a folder with name of project</p>
            <p>Inside this folder run:</p>
            <code>npx create-react-app my-app</code> <br></br>
            <code>cd my-app</code> <br></br>
            <code>npm start</code>
        </div>
    )
}
